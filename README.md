# Warehouse Ramp Task

Static participant-facing build of the Ramp warehouse coordination task.

Open `index.html` through GitHub Pages. The default build uses the `ramp-carry`
condition and does not include researcher calibration or answer-key metadata.
